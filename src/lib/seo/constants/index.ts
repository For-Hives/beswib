export { default as seoLocales } from './seo-locales.json'

// Re-export the old constants for backward compatibility
// These can be removed once all components are updated to use the new JSON file
export * from './seo-translations'
